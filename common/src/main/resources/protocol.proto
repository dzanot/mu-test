syntax = "proto3";

package rpc.pkg;


message HelloRequest {
  oneof coproduct {
    bool boolt = 1;
    int32 intt = 2;
    string stringt = 3;
  }
}

message HelloResponse {
  string message = 1;
}

service Greeter {
  rpc SayHello (HelloRequest) returns (HelloResponse);
}